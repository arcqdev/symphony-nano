# `acp-claude`

Local Claude ACP wrapper for Symphony Nano smoke and integration runs.

Install the pinned adapter dependency:

```bash
cd nano/plugins/acp-claude
npm install
```

Then use the wrapper directly:

```bash
./bin/acp-claude.mjs
```

Any extra args are forwarded to `@zed-industries/claude-agent-acp`.
